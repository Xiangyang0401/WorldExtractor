# 宇宙尽头 · 游戏设计文档

> 记录所有已明确的设计规则、玩法细则、UI 交互约定。
> 开发前必读，避免反复讨论已经决定的内容。
> 最后更新：本文档随每次开发复盘同步更新。

---

## 一、游戏基础信息

| 项目 | 内容 |
|------|------|
| 游戏名称 | 宇宙尽头 |
| 副标题 | 每一个世界，都在走向它的终点 |
| 离线模式 | 时间废墟 |
| AI模式 | 创世神典 |

---

## 二、核心玩法循环

```
游历/创世（选择或生成世界）
    ↓
进入世界，5次探索（每次做选择，影响四维属性）
    ↓
第5次后触发结局，获得世界遗物
    ↓
探索次数累积，触发"前往宇宙尽头"
    ↓
携带遗物，与虚无进行5轮对峙
    ↓
胜利：开启新周目 / 失败：重置，重新开始
```

---

## 三、探索计数与血量系统

### 两个独立字段，没有公式关系

| 字段 | 字段名 | 初始值 | 说明 |
|------|--------|--------|------|
| 存活世界数（血量） | `worldCount` | **5** | 对峙时的血量基础，不随探索增加 |
| 本周目已探索次数 | `exploredWorldCount` | **0** | 每次完成探索 +1，达到上限触发对峙 |

### 探索次数规则

| 探索次数 | 状态 |
|----------|------|
| 0 | 游历/创世正常可用，前往宇宙尽头不显示（但占位） |
| 1 | 前往宇宙尽头按钮出现（可选择提前去），游历/创世仍可用 |
| ≥ 2 | 游历/创世禁用，显示"虚无已至"，必须前往宇宙尽头 |

### 重置规则（对峙失败后）

- `exploredWorldCount` 归 0
- `worldCount` 归 5
- 遗物清空
- 周目数 +1

---

## 四、主页 UI 规则

### 整体布局

四栏布局：`280px（操作面板）| 1fr（世界简介）| 320px（世界图片）| 300px（四维属性）`
- `maxWidth: 1400`，固定高度 560px

### 左栏底部布局

- "前往宇宙尽头"按钮 + 下方三个小按钮（游历笔记 / 世界遗物 / 设置面板）**始终同时存在**，无覆盖关系
- "前往宇宙尽头"用 `visibility` 控制显隐（始终占位，不影响下方按钮位置）
- `exploredWorldCount >= 1` 时可见；`= 0` 时隐藏但占位

### 世界预览区（中栏 + 图片栏）

- 点"游历"随机抽取世界，中栏显示简介，图片栏显示缩略图
- 点"重新选择"：直接随机换一个新世界（不回到空状态）
  - 时间废墟模式：本地随机，排除当前世界
  - 创世神典模式：随机名字重新 AI 生成

### 图片栏规则

- **仅时间废墟模式**显示世界缩略图
- 创世神典模式图片栏显示占位文字"创世神典不支持图像"
- 无 thumbnail 配置时显示"此界尚无图像"
- 图片路径存储在世界数据的 `thumbnail` 字段，**路径必须以 `/` 开头**，如 `/worlds/jing-yuan.png`
- 图片文件放在 `public/worlds/` 目录下
- 图片建议正方形，格式 PNG/JPG/WEBP 均可

---

## 五、对峙系统细则

### 五轮结构

| 轮次 | 名称 | 效果 | 特殊类型 |
|------|------|------|---------|
| 第1轮 | 降临 | 摧毁 1 个世界 | — |
| 第2轮 | 侵蚀 | 随机摧毁 1 件遗物（不摧毁世界） | `erosion` |
| 第3轮 | 叩问 | 摧毁 2 个世界（可被 answer 类遗物完全抵消） | `question` |
| 第4轮 | 静默 | 摧毁 2 个世界（最多只能减免 1 个） | — |
| 第5轮 | 终焉 | 摧毁当前存活世界数的一半（向上取整），此轮遗物效果翻倍 | `final` |

### 遗物效果类型完整说明

| effectType | 具体效果 | 适用轮次 | 备注 |
|------------|---------|---------|------|
| `shield` | 抵挡摧毁 N 个世界 | 第1/3/4轮（受 maxBlock 限制） | — |
| `revive` | 复活 N 个已消亡的世界 | 任意轮 | — |
| `answer` | 完全抵消叩问伤害 | 第3轮专用 | — |
| `protect_relics` | 保护所有遗物免遭侵蚀 | 第2轮专用 | — |
| `reduce_final` | 减免终焉 N 个世界消亡 | 第5轮专用 | — |
| `auto` | 指定轮次自动触发，抵挡 N 个世界消亡 | 指定轮次自动 | 不可手动选择 |
| `delay` | 将本轮基础伤害推迟到下一轮结算 | 第1~4轮有效（第5轮无效） | 下轮双倍承受，有风险 |
| `mirror` | 照见虚无本体，完全抵消本轮全部伤害 | 任意轮 | 代价：随机失去一件其他遗物 |

### delay 效果细则

- 将本轮**基础伤害**推迟，本轮不承受伤害
- 上一轮已积累的 `pendingDelayDamage` 仍在本轮正常结算（不可二次推迟）
- 第5轮（终焉）使用无效，效果作废
- 界面上若有待结算的延迟伤害，效果说明区显示红色警告

### mirror 效果细则

- 完全抵消本轮全部伤害（含 pendingDelayDamage）
- 随机消耗一件其他**非 auto、未使用**的遗物
- 若没有其他可牺牲遗物，则只抵消伤害，无额外代价
- 适合在第4轮静默（固定2点，只能减免1点）时使用，代价换全免

### 侵蚀轮（第2轮）交互规则

- 不作为（承受侵蚀）→ 虚无随机摧毁 1 件非 auto 遗物
- 使用遗物 → 只有 `protect_relics` 类型有效，其他类型遗物变灰不可选
- 界面显示红色提示说明不作为的后果

### 对峙交互流程

```
void-speaks 阶段
    ├── 展示：虚无叙事 + 本轮效果说明
    ├── 若有 pendingDelayDamage，显示红色警告
    ├── 侵蚀轮额外显示红色提示：说明不作为后果
    ├── 按钮：「使用世界遗物」/ 「不作为」（侵蚀轮显示「承受侵蚀」）
    │
    ├── 点「使用世界遗物」→ pick-relic 阶段
    │       右栏遗物面板高亮（紫色边框发光，标题变"选择一件遗物"）
    │       可用遗物可点击，不适用/已使用遗物变灰
    │       选中后左栏底部出现「确认使用「xxx」」按钮
    │       可点「返回」取消回到 void-speaks
    │
    └── 点「不作为」→ 直接执行，进入 result 阶段

result 阶段
    ├── 只展示本轮结果（遗物叙事 + 原有世界数 → 现存世界数）
    ├── 轮次显示、世界数均用执行前保存的快照（roundSnapshot），不用已 +1 的 currentRound
    └── 按钮：「迎接第X轮」/ 「见证结局」（最后一轮后）
```

---

## 六、遗物系统

### 获取方式

- 每次探索完成，根据触发的结局获得对应遗物
- 每个结局对应一种固定遗物，effectType 不同

### 遗物展示规则

- 游历笔记页、世界遗物页、对峙右栏：均需显示具体效果说明
- 效果说明格式：直接说明数值和条件
- `auto` 类遗物在对峙右栏不可手动选择，仅展示自动触发条件

### 现有遗物完整清单

**镜渊**

| 结局 | 触发条件 | 遗物名 | 效果类型 | 效果 |
|------|---------|--------|---------|------|
| 第一次 | 共鸣≥85 且 勇气≥80 | 那张隔着水面望向你的脸 | `answer` | 完全抵消第3轮伤害 |
| 差一点 | 洞察≥80 且 共鸣≥50 | 那一寸没能触碰到的距离 | `shield` | 抵挡摧毁 1 个世界 |
| 擦肩 | 洞察<80（兜底） | 一个你没来得及看见的手势 | `auto` | 第5轮自动触发，抵挡 1 个世界消亡 |

**拾味集**

| 结局 | 触发条件 | 遗物名 | 效果类型 | 效果 |
|------|---------|--------|---------|------|
| 存在过的气味 | 追踪≥85 且 共情≥80 | 那种让所有人哭的气味 | `answer` | 完全抵消第3轮伤害 |
| 闻不到的气味 | 60≤追踪<85 | 那瓶装着你自己的小陶瓶 | `revive` | 复活 2 个已消亡的世界 |
| 无味 | 追踪<60（兜底） | 某个人路过时留下的勇气 | `reduce_final` | 减免第5轮 1 个世界消亡 |

**钢琴**

| 结局 | 触发条件 | 遗物名 | 效果类型 | 效果 |
|------|---------|--------|---------|------|
| 那首你没有写过的曲子 | 记忆≥85 且 聆听≥80 | 一张写完了的乐谱 | `protect_relics` | 保护所有遗物免遭侵蚀（第2轮） |
| 她弹琴时旁边的那个人 | 60≤聆听<85 | 侧台的位置 | `shield` | 抵挡摧毁 1 个世界 |
| 一场你没有完整听完的演出 | 聆听<60（兜底） | 你提前离开时推开的那扇侧门 | `revive` | 复活 1 个已消亡的世界 |

**大学**

| 结局 | 触发条件 | 遗物名 | 效果类型 | 效果 |
|------|---------|--------|---------|------|
| 他选择离开的那一天 | 共情≥85 且 放手≥80 | 那扇他推开的窗 | `delay` | 将本轮伤害推迟到下一轮（下轮双倍） |
| 他还在那里 | 60≤线索<85 | 下午四点那条走廊的光 | `answer` | 完全抵消第3轮伤害 |
| 你没有找到他 | 线索<60（兜底） | 那本日记的最后一页 | `revive` | 复活 2 个已消亡的世界 |

**面具**

| 结局 | 触发条件 | 遗物名 | 效果类型 | 效果 |
|------|---------|--------|---------|------|
| 你是第二个知道的人 | 洞见≥85 且 信任≥80 | 那张照见真实的面具 | `mirror` | 完全抵消本轮全部伤害，但随机失去一件其他遗物 |
| 舞会还在继续 | 60≤洞见<85 | 舞会最后一根熄灭的蜡烛 | `answer` | 完全抵消第3轮伤害 |
| 你只是一个普通的访客 | 洞见<60（兜底） | 那张你戴过的普通面具 | `shield` | 抵挡摧毁 1 个世界 |

### 遗物类型覆盖情况

| effectType | 数量 | 来源世界 |
|------------|------|---------|
| `answer` | 4 | 镜渊、拾味集、大学、面具 |
| `shield` | 3 | 镜渊、钢琴、面具 |
| `revive` | 3 | 拾味集、钢琴、大学 |
| `protect_relics` | 1 | 钢琴 ✅（已补全） |
| `auto` | 1 | 镜渊 |
| `reduce_final` | 1 | 拾味集 |
| `delay` | 1 | 大学（新） |
| `mirror` | 1 | 面具（新） |

---

## 七、属性与结局数值设计规范

### 属性系统

- 四维属性初始值均为 **50**，值域 **0~100**
- 每次选择后立即应用 attributeDeltas，超出范围自动 clamp

### 选项数值规范

| 类型 | 范围 |
|------|------|
| 主方向属性提升 | +12 到 +25 |
| 次要属性提升 | +3 到 +10 |
| 负向变化（需叙事依据） | -5 到 -15 |
| 每个选项总变化量 | 大致相当，避免某个选项明显"最优" |

### 结局阈值设计原则

**关键约束**：所有选项的属性增量几乎都是正值，5印下来每个属性净增至少 +40，初始50加上去最低也有90。因此：

- **兜底结局**的阈值不能太低，必须设置在 **80 以下**才有可能触发
- **中间结局**的区间要宽，上下界差值至少 **25**，否则很难精确落入
- **最高结局**的阈值建议在 **80~90**，需要玩家明显专注追求才能触发
- 新增世界结局时，先计算各属性的5印最低/最高累积值，再反推合理阈值

---

## 八、世界配置规范（时间废墟模式）

### 当前已有世界

| 世界名 | 文件 | 核心主题 | 图片文件 |
|--------|------|---------|---------|
| 镜渊 | `jingYuan.ts` | 真实与倒影，两界沟通 | `jing-yuan.png` |
| 拾味集 | `shiWeiJi.ts` | 气味与记忆，失去与存在 | `shi-wei-ji.png` |
| 钢琴 | `gangQin.ts` | 平行人生，另一条路的自己 | `gang-qin.png` |
| 大学 | `daxue.ts` | 最美好的时光，留下来与离开 | `da-xue.png` |
| 面具 | `mianJu.ts` | 真实的脸，秘密的代价 | `mian-ju.png` |

### 世界数据文件结构

每个预设世界一个 `.ts` 文件，位于 `src/data/worlds/`，需注册到 `index.ts`。

### 图片配置

```ts
thumbnail: '/worlds/你的世界id.png',  // 路径必须以 / 开头
```
图片放在 `public/worlds/` 目录，建议正方形，512×512 或 1024×1024。

### 新增世界时的遗物类型选择建议

参考当前覆盖情况，优先选用数量较少的类型，保持各类型均衡。

---

## 九、世界叙事书写规范

详见 `WRITING_GUIDE.md`，核心原则摘要：

1. **谜题可玩性优先**：每个世界是一个谜题，玩家通过5次探索逐渐靠近真相
2. **不堆砌辞藻**：各世界书写风格可以完全不同，不强求诗意统一
3. **可调查性**：选项对应不同调查方向，consequence 给玩家拼图，不直接给答案
4. **五印节奏**：第1印建立异常，第3印悬念顶点，第5印做关键选择
5. **NPC有动机有隐瞒**：不是信息传递工具，有自己的立场和秘密
6. **属性取舍感**：选项要有真实代价，不能全是加分项
7. **情绪不直接命名**：写身体和行为反应，不写"你感到害怕"

### 常见技术坑

- `consequence` 字符串内含单引号时，必须用模板字符串 `` ` `` 包裹，不能用 `'...'`

---

## 十、文本词条对照

| 功能 | 显示文本 |
|------|---------|
| 游戏标题 | 宇宙尽头 |
| 随机抽取世界 | 游历 |
| 自定义命名世界 | 创世 |
| 换一个世界 | 重新选择 |
| 开始探索 | 踏入此界 |
| 成就记录 | 游历笔记 |
| 道具/能力 | 世界遗物 |
| 设置 | 设置面板 |
| 离线模式 | 时间废墟 |
| AI模式 | 创世神典 |
| 结局 | 归宿 |
| 世界演变 | 余波 |
| 探索次数 | 第X印 |
| 退出探索 | 离界 |

---

## 十一、技术约定

### 状态管理

- 全局状态用 `Context + useReducer`，无第三方库
- 页面路由用 `state.page` 枚举切换，无路由库
- 持久化：设置用 `localStorage`，成就/历史/框架用 `IndexedDB`（数据库名 `wan-jie-shi-yi`）

### 对峙数据快照原则

- `executeRound` 执行后 `currentRound` 立即 +1
- result 阶段展示的轮次、世界数，必须用执行前保存的快照 `roundSnapshot`，不能用已 +1 的 `currentRound`

### delay 伤害状态

- `ConfrontationSession` 中新增 `pendingDelayDamage` 字段，记录被推迟的伤害
- 每轮执行时先加上 `pendingDelayDamage`，结算后归零
- `initConfrontationSession` 初始化时 `pendingDelayDamage: 0`

### 无界模式（创世神典）生成逻辑

- 世界框架（WorldFrame）只生成一次，永久缓存到 IndexedDB
- 探索事件每次进入时重新生成，基于历史状态上下文
- 重复进入同名世界时，AI 基于历史数据（上次结局、属性值、探索次数）生成延续性事件

### 图片路径约定

- public 目录下的文件以 `/` 为根路径
- thumbnail 字段必须写 `/worlds/xxx.png`，不能写 `worlds/xxx.png`（缺少开头斜杠会导致图片无法显示）
